# Copy to /etc/rybbit-backup.env on the Rybbit application host and chmod 600.

BACKUP_HOST=rybbit

POSTGRES_CONTAINER=postgres
POSTGRES_DATABASE=analytics
POSTGRES_STAGING_ROOT=/var/backups/rybbit/postgres
POSTGRES_RCLONE_REMOTES=b2:rybbit-db-backups/rybbit/postgres

# No ClickHouse backup is configured here. The backend uses the standalone
# ClickHouse host rather than this host's effectively empty local volume.

# POSTGRES_SUCCESS_URL=
# POSTGRES_FAILURE_URL=
