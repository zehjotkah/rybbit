# Rybbit Monitor Agent Environment Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=3003
HOST=0.0.0.0

# Region Configuration
# Use region codes like: us-east, us-west, eu-west, asia-pacific
REGION=us-east

# Security Configuration
# Set ENABLE_IP_WHITELIST=true to enable IP whitelisting (optional)
# If enabled, only IPs in ALLOWED_IPS can connect
ENABLE_IP_WHITELIST=false

# Space-separated list of IP addresses allowed to connect to this agent
# Only used if ENABLE_IP_WHITELIST=true
# These should be the IP addresses of your main Rybbit server
# Example: ALLOWED_IPS="1.2.3.4 5.6.7.8"
ALLOWED_IPS=

# Domain Configuration (for Caddy SSL)
# The domain name pointing to this monitor agent
# Example: DOMAIN_NAME=us-east.monitor.yourdomain.com
DOMAIN_NAME=

# Timeout Configuration
DEFAULT_TIMEOUT_MS=30000
MAX_TIMEOUT_MS=60000

# Logging
# Options: error, warn, info, debug
LOG_LEVEL=info

# Docker Image Configuration (for docker-compose)
# DOCKER_REGISTRY=ghcr.io
# DOCKER_IMAGE=your-org/rybbit-monitor-agent
# IMAGE_TAG=latest