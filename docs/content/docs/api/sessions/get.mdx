---
title: Get Session
description: Returns detailed information about a specific session including all events and pageviews.
full: true
method: GET
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

<div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
<div>

```
GET /api/sites/:site/sessions/:sessionId
```

Returns detailed information about a specific session including all events and pageviews.

### Path Parameters

<TypeTable
  type={{
    sessionId: {
      description: 'Session ID',
      type: 'string',
      required: true,
    },
    site: {
      description: 'Site ID or site identifier',
      type: 'string | number',
      required: true,
    },
  }}
/>

### Query Parameters

<TypeTable
  type={{
    limit: {
      description: 'Number of events to return',
      type: 'number',
      default: 100,
    },
    offset: {
      description: 'Number of events to skip',
      type: 'number',
      default: 0,
    },
    minutes: {
      description: 'Only return events from the last N minutes',
      type: 'number',
      required: false,
    },
  }}
/>

### Response

<TypeTable
  type={{
    data: {
      description: 'Session details object',
      type: 'SessionDetails',
    },
  }}
/>

#### SessionDetails Object

<TypeTable
  type={{
    session: {
      description: 'Session metadata',
      type: 'SessionMeta',
    },
    events: {
      description: 'Array of events in the session',
      type: 'Event[]',
    },
    pagination: {
      description: 'Pagination info for events',
      type: 'Pagination',
    },
  }}
/>

#### Event Object

<TypeTable
  type={{
    timestamp: {
      description: 'Event timestamp',
      type: 'string',
      typeDescription: 'ISO 8601 datetime',
    },
    pathname: {
      description: 'Page path',
      type: 'string',
    },
    hostname: {
      description: 'Hostname',
      type: 'string',
    },
    page_title: {
      description: 'Page title',
      type: 'string',
    },
    type: {
      description: 'Event type',
      type: 'string',
      typeDescription: '"pageview" | "custom_event" | "error" | "outbound"',
    },
    event_name: {
      description: 'Custom event name (for custom_event type)',
      type: 'string',
    },
    props: {
      description: 'Event properties',
      type: 'object',
    },
  }}
/>

</div>
<div className="flex flex-col gap-4">

<Tabs items={['cURL', 'JavaScript', 'Python', 'PHP', 'Ruby', 'Go', 'Rust', 'Java', '.NET']}>
  <Tab value="cURL">
```bash title="Request"
curl -X GET "https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50" \
  -H "Authorization: Bearer your_api_key_here"
```
  </Tab>
  <Tab value="JavaScript">
```javascript title="Request"
const response = await fetch(
  'https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50',
  {
    headers: {
      'Authorization': 'Bearer your_api_key_here'
    }
  }
);

const data = await response.json();
```
  </Tab>
  <Tab value="Python">
```python title="Request"
import requests

response = requests.get(
    'https://app.rybbit.io/api/sites/123/sessions/sess_abc123',
    params={'limit': 50},
    headers={
        'Authorization': 'Bearer your_api_key_here'
    }
)

data = response.json()
```
  </Tab>
  <Tab value="PHP">
```php title="Request"
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, 'https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50');
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer your_api_key_here'
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
```
  </Tab>
  <Tab value="Ruby">
```ruby title="Request"
require 'net/http'
require 'json'

uri = URI('https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50')
req = Net::HTTP::Get.new(uri)
req['Authorization'] = 'Bearer your_api_key_here'

res = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(req) }
data = JSON.parse(res.body)
```
  </Tab>
  <Tab value="Go">
```go title="Request"
req, _ := http.NewRequest("GET", "https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50", nil)
req.Header.Set("Authorization", "Bearer your_api_key_here")

client := &http.Client{}
resp, _ := client.Do(req)
defer resp.Body.Close()

var data map[string]interface{}
json.NewDecoder(resp.Body).Decode(&data)
```
  </Tab>
  <Tab value="Rust">
```rust title="Request"
let client = reqwest::Client::new();
let res = client
    .get("https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50")
    .header("Authorization", "Bearer your_api_key_here")
    .send()
    .await?;

let data: serde_json::Value = res.json().await?;
```
  </Tab>
  <Tab value="Java">
```java title="Request"
HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50"))
    .header("Authorization", "Bearer your_api_key_here")
    .GET()
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
```
  </Tab>
  <Tab value=".NET">
```csharp title="Request"
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("Authorization", "Bearer your_api_key_here");

var response = await client.GetAsync("https://app.rybbit.io/api/sites/123/sessions/sess_abc123?limit=50");
var data = await response.Content.ReadAsStringAsync();
```
  </Tab>
</Tabs>

```json title="Response"
{
  "data": {
    "session": {
      "session_id": "sess_abc123",
      "user_id": "dev_xyz789",
      "country": "US",
      "browser": "Chrome",
      "operating_system": "macOS",
      "device_type": "desktop",
      "session_start": "2024-01-31T14:00:00.000Z",
      "session_end": "2024-01-31T14:04:05.000Z",
      "session_duration": 245,
      "pageviews": 5,
      "events": 7,
      "entry_page": "/",
      "exit_page": "/pricing"
    },
    "events": [
      {
        "timestamp": "2024-01-31T14:00:00.000Z",
        "pathname": "/",
        "hostname": "example.com",
        "page_title": "Home",
        "type": "pageview",
        "referrer": "https://google.com"
      },
      {
        "timestamp": "2024-01-31T14:01:30.000Z",
        "pathname": "/",
        "hostname": "example.com",
        "type": "custom_event",
        "event_name": "cta_click",
        "props": { "button": "hero" }
      }
    ],
    "pagination": {
      "total": 7,
      "limit": 50,
      "offset": 0,
      "hasMore": false
    }
  }
}
```

</div>
</div>
