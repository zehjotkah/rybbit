---
title: Organizations
description: Manage organizations, sites, and members
full: true
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

See the [API Reference](/docs/api/getting-started) for authentication details.

## Endpoints

<div className="flex flex-col gap-2 my-6">
  <a href="#get-my-organizations" className="flex items-center gap-3 p-3 rounded-lg bg-fd-secondary/50 hover:bg-fd-secondary transition-colors no-underline">
    <span className="px-2 py-1 text-xs font-bold rounded bg-green-500/20 text-green-600 dark:text-green-400">GET</span>
    <code className="text-sm">/api/organizations</code>
  </a>
  <a href="#create-site" className="flex items-center gap-3 p-3 rounded-lg bg-fd-secondary/50 hover:bg-fd-secondary transition-colors no-underline">
    <span className="px-2 py-1 text-xs font-bold rounded bg-blue-500/20 text-blue-600 dark:text-blue-400">POST</span>
    <code className="text-sm">/api/organizations/:organizationId/sites</code>
  </a>
  <a href="#get-organization-members" className="flex items-center gap-3 p-3 rounded-lg bg-fd-secondary/50 hover:bg-fd-secondary transition-colors no-underline">
    <span className="px-2 py-1 text-xs font-bold rounded bg-green-500/20 text-green-600 dark:text-green-400">GET</span>
    <code className="text-sm">/api/organizations/:organizationId/members</code>
  </a>
  <a href="#add-organization-member" className="flex items-center gap-3 p-3 rounded-lg bg-fd-secondary/50 hover:bg-fd-secondary transition-colors no-underline">
    <span className="px-2 py-1 text-xs font-bold rounded bg-blue-500/20 text-blue-600 dark:text-blue-400">POST</span>
    <code className="text-sm">/api/organizations/:organizationId/members</code>
  </a>
</div>

---

## Get My Organizations

<div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
<div>

```
GET /api/organizations
```

Returns all organizations the authenticated user is a member of, including all members for each organization. Excludes subscription and billing information. Supports both session cookies and API key (Bearer token) authentication.

### Response

Returns an array of organization objects.

<TypeTable
  type={{
    id: {
      description: 'Organization ID',
      type: 'string',
    },
    name: {
      description: 'Organization name',
      type: 'string',
    },
    slug: {
      description: 'Organization slug',
      type: 'string',
    },
    logo: {
      description: 'Organization logo URL',
      type: 'string | null',
    },
    createdAt: {
      description: 'Organization creation timestamp',
      type: 'string',
      typeDescription: 'ISO 8601 datetime',
    },
    role: {
      description: 'Your role in the organization',
      type: 'string',
      typeDescription: '"owner" | "admin" | "member"',
    },
    members: {
      description: 'Array of organization members',
      type: 'array',
    },
    sites: {
      description: 'Array of sites in the organization',
      type: 'array',
    },
  }}
/>

#### Member Object

Each member in the `members` array contains:

<TypeTable
  type={{
    id: {
      description: 'Member record ID',
      type: 'string',
    },
    role: {
      description: 'Member role in the organization',
      type: 'string',
      typeDescription: '"owner" | "admin" | "member"',
    },
    userId: {
      description: 'User ID',
      type: 'string',
    },
    createdAt: {
      description: 'Timestamp when member was added',
      type: 'string',
      typeDescription: 'ISO 8601 datetime',
    },
    user: {
      description: 'User details',
      type: 'object',
    },
  }}
/>

#### User Object

<TypeTable
  type={{
    id: {
      description: 'User ID',
      type: 'string',
    },
    name: {
      description: 'User display name',
      type: 'string',
    },
    email: {
      description: 'User email address',
      type: 'string',
    },
  }}
/>

#### Site Object

Each site in the `sites` array contains:

<TypeTable
  type={{
    id: {
      description: 'Site ID',
      type: 'string',
    },
    domain: {
      description: 'Site domain',
      type: 'string',
    },
    name: {
      description: 'Site name',
      type: 'string',
    },
    organizationId: {
      description: 'Organization ID',
      type: 'string',
    },
    createdBy: {
      description: 'User ID who created the site',
      type: 'string',
    },
    public: {
      description: 'Whether analytics are publicly accessible',
      type: 'boolean',
    },
    saltUserIds: {
      description: 'Whether user IDs are salted',
      type: 'boolean',
    },
    blockBots: {
      description: 'Whether bot traffic is blocked',
      type: 'boolean',
    },
    createdAt: {
      description: 'Site creation timestamp',
      type: 'string',
      typeDescription: 'ISO 8601 datetime',
    },
  }}
/>

</div>
<div className="flex flex-col gap-4">

<Tabs items={['cURL', 'JavaScript', 'Python', 'PHP', 'Ruby', 'Go', 'Rust', 'Java', '.NET']}>
  <Tab value="cURL">
```bash title="Request"
curl -X GET "https://app.rybbit.io/api/organizations" \
  -H "Authorization: Bearer your_api_key_here"
```
  </Tab>
  <Tab value="JavaScript">
```javascript title="Request"
const response = await fetch(
  'https://app.rybbit.io/api/organizations',
  {
    headers: {
      'Authorization': 'Bearer your_api_key_here'
    }
  }
);

const data = await response.json();
```
  </Tab>
  <Tab value="Python">
```python title="Request"
import requests

response = requests.get(
    'https://app.rybbit.io/api/organizations',
    headers={
        'Authorization': 'Bearer your_api_key_here'
    }
)

data = response.json()
```
  </Tab>
  <Tab value="PHP">
```php title="Request"
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, "https://app.rybbit.io/api/organizations");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer your_api_key_here'
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
```
  </Tab>
  <Tab value="Ruby">
```ruby title="Request"
require 'net/http'
require 'json'

uri = URI('https://app.rybbit.io/api/organizations')
req = Net::HTTP::Get.new(uri)
req['Authorization'] = 'Bearer your_api_key_here'

res = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(req) }
data = JSON.parse(res.body)
```
  </Tab>
  <Tab value="Go">
```go title="Request"
req, _ := http.NewRequest("GET", "https://app.rybbit.io/api/organizations", nil)
req.Header.Set("Authorization", "Bearer your_api_key_here")

client := &http.Client{}
resp, _ := client.Do(req)
defer resp.Body.Close()

var data []map[string]interface{}
json.NewDecoder(resp.Body).Decode(&data)
```
  </Tab>
  <Tab value="Rust">
```rust title="Request"
let client = reqwest::Client::new();
let res = client
    .get("https://app.rybbit.io/api/organizations")
    .header("Authorization", "Bearer your_api_key_here")
    .send()
    .await?;

let data: serde_json::Value = res.json().await?;
```
  </Tab>
  <Tab value="Java">
```java title="Request"
HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://app.rybbit.io/api/organizations"))
    .header("Authorization", "Bearer your_api_key_here")
    .GET()
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
```
  </Tab>
  <Tab value=".NET">
```csharp title="Request"
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("Authorization", "Bearer your_api_key_here");

var response = await client.GetAsync("https://app.rybbit.io/api/organizations");
var data = await response.Content.ReadAsStringAsync();
```
  </Tab>
</Tabs>

```json title="Response"
[
  {
    "id": "org_123",
    "name": "Acme Inc",
    "slug": "acme-inc",
    "logo": "https://example.com/logo.png",
    "createdAt": "2024-01-15T10:30:00.000Z",
    "role": "owner",
    "members": [
      {
        "id": "member_abc123",
        "role": "owner",
        "userId": "user_xyz789",
        "createdAt": "2024-01-15T10:30:00.000Z",
        "user": {
          "id": "user_xyz789",
          "name": "John Doe",
          "email": "john@example.com"
        }
      },
      {
        "id": "member_def456",
        "role": "member",
        "userId": "user_abc456",
        "createdAt": "2024-02-20T14:15:00.000Z",
        "user": {
          "id": "user_abc456",
          "name": "Jane Smith",
          "email": "jane@example.com"
        }
      }
    ],
    "sites": [
      {
        "id": "456",
        "domain": "example.com",
        "name": "My Website",
        "organizationId": "org_123",
        "createdBy": "user_xyz789",
        "public": false,
        "saltUserIds": false,
        "blockBots": true,
        "createdAt": "2024-01-15T10:30:00.000Z"
      }
    ]
  },
  {
    "id": "org_456",
    "name": "Tech Startup",
    "slug": "tech-startup",
    "logo": null,
    "createdAt": "2024-03-10T08:00:00.000Z",
    "role": "admin",
    "members": [
      {
        "id": "member_ghi789",
        "role": "owner",
        "userId": "user_owner123",
        "createdAt": "2024-03-10T08:00:00.000Z",
        "user": {
          "id": "user_owner123",
          "name": "Alice Johnson",
          "email": "alice@techstartup.com"
        }
      },
      {
        "id": "member_jkl012",
        "role": "admin",
        "userId": "user_xyz789",
        "createdAt": "2024-03-12T09:30:00.000Z",
        "user": {
          "id": "user_xyz789",
          "name": "John Doe",
          "email": "john@example.com"
        }
      }
    ],
    "sites": [
      {
        "id": "789",
        "domain": "techstartup.com",
        "name": "Tech Startup Website",
        "organizationId": "org_456",
        "createdBy": "user_owner123",
        "public": true,
        "saltUserIds": false,
        "blockBots": true,
        "createdAt": "2024-03-10T08:00:00.000Z"
      }
    ]
  }
]
```

</div>
</div>

---

## Create Site

<div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
<div>

```
POST /api/organizations/:organizationId/sites
```

Creates a new site within an organization. Requires admin or owner role in the organization.

### Path Parameters

<TypeTable
  type={{
    organizationId: {
      description: 'Organization ID to add the site to',
      type: 'string',
      required: true,
    },
  }}
/>

### Request Body

<TypeTable
  type={{
    domain: {
      description: 'Site domain',
      type: 'string',
      typeDescription: 'Valid domain format (e.g., example.com)',
      required: true,
    },
    name: {
      description: 'Site display name',
      type: 'string',
      required: true,
    },
    public: {
      description: 'Make analytics publicly accessible',
      type: 'boolean',
      default: false,
    },
    saltUserIds: {
      description: 'Salt user IDs for privacy',
      type: 'boolean',
      default: false,
    },
    blockBots: {
      description: 'Block bot traffic',
      type: 'boolean',
      default: true,
    },
    excludedIPs: {
      description: 'Array of IP addresses to exclude from tracking',
      type: 'string[]',
      default: '[]',
    },
    excludedCountries: {
      description: 'Array of ISO country codes to exclude (e.g., ["US", "GB"])',
      type: 'string[]',
      default: '[]',
    },
    sessionReplay: {
      description: 'Enable session replay recording',
      type: 'boolean',
      default: false,
    },
    webVitals: {
      description: 'Enable Core Web Vitals tracking',
      type: 'boolean',
      default: false,
    },
    trackErrors: {
      description: 'Enable JavaScript error tracking',
      type: 'boolean',
      default: false,
    },
    trackOutbound: {
      description: 'Track outbound link clicks',
      type: 'boolean',
      default: true,
    },
    trackUrlParams: {
      description: 'Track URL parameters (UTM, etc.)',
      type: 'boolean',
      default: true,
    },
    trackInitialPageView: {
      description: 'Automatically track the initial page view',
      type: 'boolean',
      default: true,
    },
    trackSpaNavigation: {
      description: 'Track SPA navigation events',
      type: 'boolean',
      default: true,
    },
    trackIp: {
      description: 'Store visitor IP addresses',
      type: 'boolean',
      default: false,
    },
    trackButtonClicks: {
      description: 'Track button click events',
      type: 'boolean',
      default: false,
    },
    trackCopy: {
      description: 'Track copy events',
      type: 'boolean',
      default: false,
    },
    trackFormInteractions: {
      description: 'Track form interaction events',
      type: 'boolean',
      default: false,
    },
    tags: {
      description: 'Array of tags for organizing the site',
      type: 'string[]',
      default: '[]',
    },
  }}
/>

### Response

Returns the created site object with the assigned `siteId`.

<TypeTable
  type={{
    siteId: {
      description: 'Numeric site ID (auto-assigned)',
      type: 'number',
    },
    id: {
      description: 'Site UUID',
      type: 'string',
    },
    domain: {
      description: 'Site domain',
      type: 'string',
    },
    name: {
      description: 'Site name',
      type: 'string',
    },
    organizationId: {
      description: 'Organization ID',
      type: 'string',
    },
    createdBy: {
      description: 'User ID who created the site',
      type: 'string',
    },
    public: {
      description: 'Whether analytics are public',
      type: 'boolean',
    },
    saltUserIds: {
      description: 'Whether user IDs are salted',
      type: 'boolean',
    },
    blockBots: {
      description: 'Whether bot traffic is blocked',
      type: 'boolean',
    },
    excludedIPs: {
      description: 'Excluded IP addresses',
      type: 'string[]',
    },
    excludedCountries: {
      description: 'Excluded country codes',
      type: 'string[]',
    },
    sessionReplay: {
      description: 'Whether session replay is enabled',
      type: 'boolean',
    },
    webVitals: {
      description: 'Whether Web Vitals tracking is enabled',
      type: 'boolean',
    },
    trackErrors: {
      description: 'Whether error tracking is enabled',
      type: 'boolean',
    },
    trackOutbound: {
      description: 'Whether outbound link tracking is enabled',
      type: 'boolean',
    },
    trackUrlParams: {
      description: 'Whether URL parameter tracking is enabled',
      type: 'boolean',
    },
    trackInitialPageView: {
      description: 'Whether initial page view is tracked automatically',
      type: 'boolean',
    },
    trackSpaNavigation: {
      description: 'Whether SPA navigation is tracked',
      type: 'boolean',
    },
    trackIp: {
      description: 'Whether IP addresses are stored',
      type: 'boolean',
    },
    trackButtonClicks: {
      description: 'Whether button clicks are tracked',
      type: 'boolean',
    },
    trackCopy: {
      description: 'Whether copy events are tracked',
      type: 'boolean',
    },
    trackFormInteractions: {
      description: 'Whether form interactions are tracked',
      type: 'boolean',
    },
    tags: {
      description: 'Site tags',
      type: 'string[]',
    },
  }}
/>

</div>
<div className="flex flex-col gap-4">

<Tabs items={['cURL', 'JavaScript', 'Python', 'PHP', 'Ruby', 'Go', 'Rust', 'Java', '.NET']}>
  <Tab value="cURL">
```bash title="Request"
curl -X POST "https://app.rybbit.io/api/organizations/org_123/sites" \
  -H "Authorization: Bearer your_api_key_here" \
  -H "Content-Type: application/json" \
  -d '{"domain": "example.com", "name": "My Website", "blockBots": true}'
```
  </Tab>
  <Tab value="JavaScript">
```javascript title="Request"
const organizationId = 'org_123';
const response = await fetch(
  `https://app.rybbit.io/api/organizations/${organizationId}/sites`,
  {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer your_api_key_here',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      domain: 'example.com',
      name: 'My Website',
      blockBots: true
    })
  }
);

const data = await response.json();
```
  </Tab>
  <Tab value="Python">
```python title="Request"
import requests

organization_id = 'org_123'
response = requests.post(
    f'https://app.rybbit.io/api/organizations/{organization_id}/sites',
    json={
        'domain': 'example.com',
        'name': 'My Website',
        'blockBots': True
    },
    headers={
        'Authorization': 'Bearer your_api_key_here'
    }
)

data = response.json()
```
  </Tab>
  <Tab value="PHP">
```php title="Request"
$organizationId = 'org_123';
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, "https://app.rybbit.io/api/organizations/{$organizationId}/sites");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
    'domain' => 'example.com',
    'name' => 'My Website',
    'blockBots' => true
]));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer your_api_key_here',
    'Content-Type: application/json'
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
```
  </Tab>
  <Tab value="Ruby">
```ruby title="Request"
require 'net/http'
require 'json'

organization_id = 'org_123'
uri = URI("https://app.rybbit.io/api/organizations/#{organization_id}/sites")
req = Net::HTTP::Post.new(uri)
req['Authorization'] = 'Bearer your_api_key_here'
req['Content-Type'] = 'application/json'
req.body = {
  domain: 'example.com',
  name: 'My Website',
  blockBots: true
}.to_json

res = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(req) }
data = JSON.parse(res.body)
```
  </Tab>
  <Tab value="Go">
```go title="Request"
organizationId := "org_123"
body := bytes.NewBuffer([]byte(`{
  "domain": "example.com",
  "name": "My Website",
  "blockBots": true
}`))
req, _ := http.NewRequest("POST", "https://app.rybbit.io/api/organizations/"+organizationId+"/sites", body)
req.Header.Set("Authorization", "Bearer your_api_key_here")
req.Header.Set("Content-Type", "application/json")

client := &http.Client{}
resp, _ := client.Do(req)
defer resp.Body.Close()

var data map[string]interface{}
json.NewDecoder(resp.Body).Decode(&data)
```
  </Tab>
  <Tab value="Rust">
```rust title="Request"
let organization_id = "org_123";
let client = reqwest::Client::new();
let res = client
    .post(format!("https://app.rybbit.io/api/organizations/{}/sites", organization_id))
    .header("Authorization", "Bearer your_api_key_here")
    .json(&serde_json::json!({
        "domain": "example.com",
        "name": "My Website",
        "blockBots": true
    }))
    .send()
    .await?;

let data: serde_json::Value = res.json().await?;
```
  </Tab>
  <Tab value="Java">
```java title="Request"
String organizationId = "org_123";
HttpClient client = HttpClient.newHttpClient();
String json = "{\"domain\": \"example.com\", \"name\": \"My Website\", \"blockBots\": true}";
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://app.rybbit.io/api/organizations/" + organizationId + "/sites"))
    .header("Authorization", "Bearer your_api_key_here")
    .header("Content-Type", "application/json")
    .POST(HttpRequest.BodyPublishers.ofString(json))
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
```
  </Tab>
  <Tab value=".NET">
```csharp title="Request"
var organizationId = "org_123";
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("Authorization", "Bearer your_api_key_here");

var content = new StringContent(
    "{\"domain\": \"example.com\", \"name\": \"My Website\", \"blockBots\": true}",
    Encoding.UTF8,
    "application/json"
);

var response = await client.PostAsync($"https://app.rybbit.io/api/organizations/{organizationId}/sites", content);
var data = await response.Content.ReadAsStringAsync();
```
  </Tab>
</Tabs>

```json title="Response"
{
  "siteId": 456,
  "id": "a1b2c3d4e5f6",
  "domain": "example.com",
  "name": "My Website",
  "organizationId": "org_123",
  "createdBy": "user_xyz789",
  "public": false,
  "saltUserIds": false,
  "blockBots": true,
  "excludedIPs": [],
  "excludedCountries": [],
  "sessionReplay": false,
  "webVitals": false,
  "trackErrors": false,
  "trackOutbound": true,
  "trackUrlParams": true,
  "trackInitialPageView": true,
  "trackSpaNavigation": true,
  "trackIp": false,
  "trackButtonClicks": false,
  "trackCopy": false,
  "trackFormInteractions": false,
  "tags": []
}
```

</div>
</div>

---

## Get Organization Members

<div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
<div>

```
GET /api/organizations/:organizationId/members
```

Returns all members of an organization with their user details and roles. Requires membership in the organization.

### Path Parameters

<TypeTable
  type={{
    organizationId: {
      description: 'Organization ID',
      type: 'string',
      required: true,
    },
  }}
/>

### Response

<TypeTable
  type={{
    success: {
      description: 'Whether the request was successful',
      type: 'boolean',
    },
    data: {
      description: 'Array of organization members',
      type: 'array',
    },
  }}
/>

#### Member Object

Each member in the `data` array contains:

<TypeTable
  type={{
    id: {
      description: 'Member record ID',
      type: 'string',
    },
    role: {
      description: 'Member role in the organization',
      type: 'string',
      typeDescription: '"owner" | "admin" | "member"',
    },
    userId: {
      description: 'User ID',
      type: 'string',
    },
    organizationId: {
      description: 'Organization ID',
      type: 'string',
    },
    createdAt: {
      description: 'Timestamp when member was added',
      type: 'string',
      typeDescription: 'ISO 8601 datetime',
    },
    user: {
      description: 'User details',
      type: 'object',
    },
  }}
/>

#### User Object

<TypeTable
  type={{
    id: {
      description: 'User ID',
      type: 'string',
    },
    name: {
      description: 'User display name',
      type: 'string',
    },
    email: {
      description: 'User email address',
      type: 'string',
    },
  }}
/>

</div>
<div className="flex flex-col gap-4">

<Tabs items={['cURL', 'JavaScript', 'Python', 'PHP', 'Ruby', 'Go', 'Rust', 'Java', '.NET']}>
  <Tab value="cURL">
```bash title="Request"
curl -X GET "https://app.rybbit.io/api/organizations/org_123/members" \
  -H "Authorization: Bearer your_api_key_here"
```
  </Tab>
  <Tab value="JavaScript">
```javascript title="Request"
const organizationId = 'org_123';
const response = await fetch(
  `https://app.rybbit.io/api/organizations/${organizationId}/members`,
  {
    headers: {
      'Authorization': 'Bearer your_api_key_here'
    }
  }
);

const data = await response.json();
```
  </Tab>
  <Tab value="Python">
```python title="Request"
import requests

organization_id = 'org_123'
response = requests.get(
    f'https://app.rybbit.io/api/organizations/{organization_id}/members',
    headers={
        'Authorization': 'Bearer your_api_key_here'
    }
)

data = response.json()
```
  </Tab>
  <Tab value="PHP">
```php title="Request"
$organizationId = 'org_123';
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, "https://app.rybbit.io/api/organizations/{$organizationId}/members");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer your_api_key_here'
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
```
  </Tab>
  <Tab value="Ruby">
```ruby title="Request"
require 'net/http'
require 'json'

organization_id = 'org_123'
uri = URI("https://app.rybbit.io/api/organizations/#{organization_id}/members")
req = Net::HTTP::Get.new(uri)
req['Authorization'] = 'Bearer your_api_key_here'

res = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(req) }
data = JSON.parse(res.body)
```
  </Tab>
  <Tab value="Go">
```go title="Request"
organizationId := "org_123"
req, _ := http.NewRequest("GET", "https://app.rybbit.io/api/organizations/"+organizationId+"/members", nil)
req.Header.Set("Authorization", "Bearer your_api_key_here")

client := &http.Client{}
resp, _ := client.Do(req)
defer resp.Body.Close()

var data map[string]interface{}
json.NewDecoder(resp.Body).Decode(&data)
```
  </Tab>
  <Tab value="Rust">
```rust title="Request"
let organization_id = "org_123";
let client = reqwest::Client::new();
let res = client
    .get(format!("https://app.rybbit.io/api/organizations/{}/members", organization_id))
    .header("Authorization", "Bearer your_api_key_here")
    .send()
    .await?;

let data: serde_json::Value = res.json().await?;
```
  </Tab>
  <Tab value="Java">
```java title="Request"
String organizationId = "org_123";
HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://app.rybbit.io/api/organizations/" + organizationId + "/members"))
    .header("Authorization", "Bearer your_api_key_here")
    .GET()
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
```
  </Tab>
  <Tab value=".NET">
```csharp title="Request"
var organizationId = "org_123";
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("Authorization", "Bearer your_api_key_here");

var response = await client.GetAsync($"https://app.rybbit.io/api/organizations/{organizationId}/members");
var data = await response.Content.ReadAsStringAsync();
```
  </Tab>
</Tabs>

```json title="Response"
{
  "success": true,
  "data": [
    {
      "id": "member_abc123",
      "role": "owner",
      "userId": "user_xyz789",
      "organizationId": "org_123",
      "createdAt": "2024-01-15T10:30:00.000Z",
      "user": {
        "id": "user_xyz789",
        "name": "John Doe",
        "email": "john@example.com"
      }
    },
    {
      "id": "member_def456",
      "role": "member",
      "userId": "user_abc456",
      "organizationId": "org_123",
      "createdAt": "2024-02-20T14:15:00.000Z",
      "user": {
        "id": "user_abc456",
        "name": "Jane Smith",
        "email": "jane@example.com"
      }
    }
  ]
}
```

</div>
</div>

---

## Add Organization Member

<div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
<div>

```
POST /api/organizations/:organizationId/members
```

Adds a user to an organization with a specified role. The user must already exist in the system. Requires admin or owner role in the organization.

### Path Parameters

<TypeTable
  type={{
    organizationId: {
      description: 'Organization ID',
      type: 'string',
      required: true,
    },
  }}
/>

### Request Body

<TypeTable
  type={{
    email: {
      description: 'Email address of the user to add',
      type: 'string',
      typeDescription: 'Must match an existing user',
      required: true,
    },
    role: {
      description: 'Role to assign to the user',
      type: 'string',
      typeDescription: '"admin" | "member" | "owner"',
      required: true,
    },
  }}
/>

### Response

Returns a success message when the user is added successfully.

<TypeTable
  type={{
    message: {
      description: 'Success message',
      type: 'string',
    },
  }}
/>

### Error Responses

<TypeTable
  type={{
    '400': {
      description: 'Invalid input (missing fields, invalid role, or user already a member)',
      type: 'object',
    },
    '401': {
      description: 'Unauthorized (not an admin/owner of the organization)',
      type: 'object',
    },
    '404': {
      description: 'User not found',
      type: 'object',
    },
    '500': {
      description: 'Internal server error',
      type: 'object',
    },
  }}
/>

</div>
<div className="flex flex-col gap-4">

<Tabs items={['cURL', 'JavaScript', 'Python', 'PHP', 'Ruby', 'Go', 'Rust', 'Java', '.NET']}>
  <Tab value="cURL">
```bash title="Request"
curl -X POST "https://app.rybbit.io/api/organizations/org_123/members" \
  -H "Authorization: Bearer your_api_key_here" \
  -H "Content-Type: application/json" \
  -d '{"email": "newuser@example.com", "role": "member"}'
```
  </Tab>
  <Tab value="JavaScript">
```javascript title="Request"
const organizationId = 'org_123';
const response = await fetch(
  `https://app.rybbit.io/api/organizations/${organizationId}/members`,
  {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer your_api_key_here',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      email: 'newuser@example.com',
      role: 'member'
    })
  }
);

const data = await response.json();
```
  </Tab>
  <Tab value="Python">
```python title="Request"
import requests

organization_id = 'org_123'
response = requests.post(
    f'https://app.rybbit.io/api/organizations/{organization_id}/members',
    json={
        'email': 'newuser@example.com',
        'role': 'member'
    },
    headers={
        'Authorization': 'Bearer your_api_key_here'
    }
)

data = response.json()
```
  </Tab>
  <Tab value="PHP">
```php title="Request"
$organizationId = 'org_123';
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, "https://app.rybbit.io/api/organizations/{$organizationId}/members");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
    'email' => 'newuser@example.com',
    'role' => 'member'
]));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer your_api_key_here',
    'Content-Type: application/json'
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
```
  </Tab>
  <Tab value="Ruby">
```ruby title="Request"
require 'net/http'
require 'json'

organization_id = 'org_123'
uri = URI("https://app.rybbit.io/api/organizations/#{organization_id}/members")
req = Net::HTTP::Post.new(uri)
req['Authorization'] = 'Bearer your_api_key_here'
req['Content-Type'] = 'application/json'
req.body = {
  email: 'newuser@example.com',
  role: 'member'
}.to_json

res = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) { |http| http.request(req) }
data = JSON.parse(res.body)
```
  </Tab>
  <Tab value="Go">
```go title="Request"
organizationId := "org_123"
body := bytes.NewBuffer([]byte(`{
  "email": "newuser@example.com",
  "role": "member"
}`))
req, _ := http.NewRequest("POST", "https://app.rybbit.io/api/organizations/"+organizationId+"/members", body)
req.Header.Set("Authorization", "Bearer your_api_key_here")
req.Header.Set("Content-Type", "application/json")

client := &http.Client{}
resp, _ := client.Do(req)
defer resp.Body.Close()

var data map[string]interface{}
json.NewDecoder(resp.Body).Decode(&data)
```
  </Tab>
  <Tab value="Rust">
```rust title="Request"
let organization_id = "org_123";
let client = reqwest::Client::new();
let res = client
    .post(format!("https://app.rybbit.io/api/organizations/{}/members", organization_id))
    .header("Authorization", "Bearer your_api_key_here")
    .json(&serde_json::json!({
        "email": "newuser@example.com",
        "role": "member"
    }))
    .send()
    .await?;

let data: serde_json::Value = res.json().await?;
```
  </Tab>
  <Tab value="Java">
```java title="Request"
String organizationId = "org_123";
HttpClient client = HttpClient.newHttpClient();
String json = "{\"email\": \"newuser@example.com\", \"role\": \"member\"}";
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://app.rybbit.io/api/organizations/" + organizationId + "/members"))
    .header("Authorization", "Bearer your_api_key_here")
    .header("Content-Type", "application/json")
    .POST(HttpRequest.BodyPublishers.ofString(json))
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
```
  </Tab>
  <Tab value=".NET">
```csharp title="Request"
var organizationId = "org_123";
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("Authorization", "Bearer your_api_key_here");

var content = new StringContent(
    "{\"email\": \"newuser@example.com\", \"role\": \"member\"}",
    Encoding.UTF8,
    "application/json"
);

var response = await client.PostAsync($"https://app.rybbit.io/api/organizations/{organizationId}/members", content);
var data = await response.Content.ReadAsStringAsync();
```
  </Tab>
</Tabs>

```json title="Response"
{
  "message": "User added to organization successfully"
}
```

```json title="Error Response (400 - Already a Member)"
{
  "error": "User is already a member of this organization"
}
```

```json title="Error Response (404 - User Not Found)"
{
  "error": "User not found"
}
```

```json title="Error Response (401 - Unauthorized)"
{
  "error": "Unauthorized"
}
```

</div>
</div>
