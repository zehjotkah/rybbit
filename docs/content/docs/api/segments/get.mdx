---
title: Get Segment
description: Returns one saved segment.
full: true
method: GET
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

<div className="grid grid-cols-1 xl:grid-cols-2 gap-8">
<div>

```
GET /api/sites/:site/segments/:segmentId
```

Returns one saved segment.

### Path Parameters

<TypeTable
  type={{
    site: {
      description: 'Site ID or site identifier',
      type: 'string | number',
      required: true,
    },
    segmentId: {
      description: 'Segment ID',
      type: 'number',
      required: true,
    },
  }}
/>

### Response

Returns the segment.

<TypeTable
  type={{
    segmentId: { description: 'Segment ID', type: 'number' },
    siteId: { description: 'Site ID, or null for an organization-wide segment', type: 'number | null' },
    organizationId: { description: 'Owning organization', type: 'string' },
    userId: { description: 'Creator, or null once that account is deleted', type: 'string | null' },
    name: { description: 'Segment name', type: 'string' },
    description: { description: 'Notes', type: 'string | null' },
    filters: { description: 'The filters that define the segment', type: 'Filter[]' },
    isPublic: { description: 'Shown on the public dashboard and private links', type: 'boolean' },
    type: { description: 'Always "segment" today; reserved for cohorts', type: 'string' },
    canEdit: { description: 'Whether the caller may edit or delete this segment', type: 'boolean' },
    createdAt: { description: 'ISO 8601 timestamp', type: 'string' },
    updatedAt: { description: 'ISO 8601 timestamp', type: 'string' },
  }}
/>

</div>
<div className="flex flex-col gap-4">

<Tabs items={['cURL', 'JavaScript', 'Python']}>
  <Tab value="cURL">
```bash title="Request"
curl -X GET "https://app.rybbit.io/api/sites/1/segments/12" \
  -H "Authorization: Bearer your_api_key_here"
```
  </Tab>
  <Tab value="JavaScript">
```javascript title="Request"
const response = await fetch(
  'https://app.rybbit.io/api/sites/1/segments/12',
  {
    method: 'GET',
    headers: {
      'Authorization': 'Bearer your_api_key_here'
    }
  }
);

const data = await response.json();
```
  </Tab>
  <Tab value="Python">
```python title="Request"
import requests

response = requests.get(
    'https://app.rybbit.io/api/sites/1/segments/12',
    headers={'Authorization': 'Bearer your_api_key_here'}
)

data = response.json()
```
  </Tab>
</Tabs>

```json title="Response"
{
  "segmentId": 12,
  "siteId": 1,
  "organizationId": "org_abc123",
  "userId": "user_xyz",
  "name": "Mobile organic from Germany",
  "description": null,
  "filters": [
    { "parameter": "device_type", "type": "equals", "value": ["Mobile"] },
    { "parameter": "channel", "type": "equals", "value": ["Organic Search"] },
    { "parameter": "country", "type": "equals", "value": ["DE"] }
  ],
  "isPublic": false,
  "type": "segment",
  "canEdit": true,
  "createdAt": "2026-09-02T10:15:00.000Z",
  "updatedAt": "2026-09-02T10:15:00.000Z"
}
```

</div>
</div>
